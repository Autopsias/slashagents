---
description: "Alias for cc-agents-commands:parallelize-agents"
---

# /parallelize-agents (Alias)

This is an alias that delegates to the namespaced plugin command.

Use the Skill tool to invoke: `cc-agents-commands:parallelize-agents`

Pass through any arguments: $ARGUMENTS
